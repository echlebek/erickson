<!DOCTYPE html>
<html lang="en">
  <head>
	<meta name="viewport" content="width=device-width, initial-scale=1">
	{{ range $k, $v := .Stylesheets }}
	<link rel="stylesheet" href="/assets/{{ $k }}">
	{{ end }}

	{{ range $k, $v := .Scripts }}
	<script src="/assets/{{ $k }}"></script>
	{{ end }}
  </head>
  <body>
  	<nav class="clearfix white bg-black">
	  <div class="sm-col">
		<a class="btn aqua mt1 mb1 ml1" href="/">Erickson Code Review</a>
      </div>
      <div id="navbar" class="navbar-collapse collapse">
        <div class="sm-col-right">
          <button onClick="createReview()" class="btn btn-outline mb1 mt1 mr1 btn-submit">Create Review</button>
          <button onClick="cancelReview()" class="btn btn-outline mb1 mt1 mr1 btn-discard hidden" id="cancel-button">Cancel</button>
        </div>
      </div>
    </nav>
    <div class="jumbotron">
      <div class="container create-review hidden" id="create-review">
        <h3>Create a new review by uploading a diff. It's easy!</h3>
        <form method="post" action="/reviews">
          <div class="form-group">
            <textarea class="hidden" name="diff" id="diff"></textarea>
          </div>
          <div class="flex">
            <input class="form-control" type="file" onchange="pasteFile(this.files[0])">
          </div>
          <div class="form-group">
            <label>Tell me a bit about the review, and yourself</label>
			<div><input class="block col-12 field" type="text" name="commitmsg" placeholder="Commit Message"></div>
            <input class="block col-12 field" type="text" name="username" placeholder="User Name">
            <input class="block col-12 field" type="text" name="repository" placeholder="Repository Name">
          </div>
          <button type="submit" class="btn btn-outline">Submit</button> 
        </form>
      </div>
      <div class="container">
        <h2>Reviews</h2>
        <div class="checkbox">
          <label>
            <input id="show-all" onClick="toggleShowAll()" type="checkbox" value="false">Show Submitted and Discarded Reviews</input>
          </label>
        </div>
        <table class="table table-hover">
          <thead>
            <th>Review</th>
            <th>Repository</th>
            <th>Submitter</th>
            <th>Submitted At</th>
            <th>Status</th>
          </thead>
          <tbody>
            {{range $i, $v := .Reviews}}
            <tr>
              <td><a href="{{$v.URL}}"><span title="{{$v.CommitMsg}}" class="label label-primary">Review {{$v.ID}}</span></a></td>
              <td>{{$v.Repository}}</td>
              <td>{{$v.Submitter}}</td>
              <td>{{$v.SubmittedAt}}</td>
              <td><span id="status-{{$i}}" class="label {{$v.StatusLabel}}">{{$v.Status}}</span></td>
            </tr>
            {{end}}
          <tbody>
        </table>
      </div>
    </div>
  </body>
</html>
